# Rust build output
/src-tauri/target/
/src-tauri/permissions/autogenerated/

# JavaScript dependencies and local tooling
/node_modules/
/.npm/

# IDE and editor files
/.idea/
/.vscode/
*.code-workspace

# Local environment and machine-specific files
.env
.env.*
*.local
*.log

# Local release staging. Upload these files to GitHub Releases instead of
# committing binaries to the repository history.
release-assets/**/*.exe
release-assets/**/*.msi
release-assets/**/*.AppImage
release-assets/**/*.deb
release-assets/**/*.dmg
release-assets/**/*.tar.gz
!release-assets/**/README.md

# Windows/macOS metadata
Thumbs.db
.DS_Store

# Nested repository artifact (accidental clone inside the repo root)
/IG-Now/
