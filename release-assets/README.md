# Local release assets

IG-Now is authored and maintained solely by
[@benedictusrey](https://github.com/benedictusrey).

Local upload staging area, recovered from the original
`Documents\Codex\2026-08-02\i\outputs` workspace. Executables and packages
are ignored by Git (`.gitignore`) and must be attached to GitHub Releases
instead of being committed to repository history.

## v1.0.0 (pre-release variants)

The published `v1.0.0` GitHub Release carries the final cross-platform
assets (`IG-Now_1.0.0_x64-setup.exe`, `IG-Now_1.0.0_x64_en-US.msi`, plus
Linux and macOS packages). This folder holds the **earlier, unpublished**
Windows intermediates from the 1.0.0 line (`mute-dom-seek-fixed` era):

- `IG-Now-1.0.0-Windows-x64-mute-dom-seek-fixed-setup.exe` — NSIS installer
- `IG-Now-1.0.0-Windows-x64-mute-dom-seek-fixed.exe` — portable executable
- `IG-Now-1.0.0-Windows-x64-mute-dom-seek-fixed.msi` — MSI package

They are preserved for reference only — do not attach them to any release;
use the current release's CI-produced assets instead.
